{
  "name": "backend-service",
  "version": "1.0.0",
  "type": "module",
  "description": "Minimal backend service skeleton",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "echo \"No tests yet\" && exit 0"
  },
  "dependencies": {
    "fastify": "^4.26.0",
    "pino-pretty": "^10.3.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}